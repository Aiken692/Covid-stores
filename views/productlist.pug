<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Product list
        link(rel="stylesheet", href="/main css/bootstrap.min.css")
    body
        h2(style="text-align: center;") Added Products.
        .container
            table.table
                thead.thead-dark
                    tr
                        th Product Name
                        th Category
                        th Make
                        th Date of entry
                        th Serial number
                        th Price
                        th Initial Pay
                        th Pay Interval
                        th Color
                        th Number in Stock
                        th Description
                        th Image 
                tbody

                    each product in products


                        tr
                            td #{product.productName}
                            td #{product.category}
                            td #{product.make}
                            td #{product.date}
                            td #{product.serialNo}
                            td #{product.price}
                            td #{product.initial_pay}
                            td #{product.pay_interval}
                            td #{product.color}
                            td #{product.stockNumber}
                            td #{product.description}                                                                                                   
                            td 
                                img(src=product.image, alt="no image", srcset="")
                            td
                            td
                                form(method="post" action='/Addpdt/delete')
                                    input(type="hidden", placeholder="", name="id", value=product._id)
                                    button.btn.btn-dark.btn-block(type="submit") Delete 


    button.btn.btn-primary#add
        a(href="/Addpdt", style="color: white") Add New Product.
    br
    footer(#footer,style="color: white; background-color: rgb(6,6,22); text-align: center;" class="fixed-bottom") Copyright © 2020 All rights reserved by Walter.